{
  "name": "testathon",
  "version": "1.0.0",
  "description": "Playwright API tests for Restful Booker",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
